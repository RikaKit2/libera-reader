use gpui::{Pixels, px};

/// Padding around the setup page container (24px).
pub const SETUP_PAGE_PADDING: Pixels = px(24.0);

/// Maximum content width for standard setup steps (480px).
pub const MAX_CONTENT_WIDTH: Pixels = px(480.0);

/// Maximum content width for welcome step (520px).
pub const MAX_WELCOME_WIDTH: Pixels = px(520.0);

/// Icon size on setup cards (48px).
pub const SETUP_ICON_SIZE: Pixels = px(48.0);

/// Select dropdown width on appearance step (240px).
pub const APPEARANCE_SELECT_WIDTH: Pixels = px(240.0);
