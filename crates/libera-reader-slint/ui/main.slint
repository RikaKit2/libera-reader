import { App } from "pages/app/app.slint";
import { BookViewer } from "pages/book_viewer/book_viewer.slint";
import {Rotes} from "vars.slint";


export component AppWindow inherits Window {
    in property <bool> setup_ready: true;
    if setup_ready: App {
        Height: root.height;
        Width: root.width;
    }
    if !setup_ready:  BookViewer { }
}
