import { Button, VerticalBox } from "std-widgets.slint";
import { Setup } from "pages/setup.slint";
import { Main } from "pages/main/body.slint";


export component AppWindow inherits Window {
    min-height: 500px;
    min-width: 500px;
    in property <bool> is_setup: false;
    if is_setup:  Setup { }
    if !is_setup: Main { }
}
