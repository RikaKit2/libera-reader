import { Global } from "../vars.slint";

export component Setup {
    height: 100%;
    width: 100%;
    Rectangle {
        background: Global.curr_theme.bg-300;
        Text {
            font-size: 20px;
            color: white;
            text: "some";
        }
    }
}
